---
date: 2026-05-22
projeto: Gestou
tipo: log-tecnico
tags: [fea, prd, docs, rpa, lider-rh, reconciliacao, fea-011, download, zip, gcs, deploy]
---

# Log Técnico — 2026-05-22

## Sessão 1: Reconciliação da FEA-009 (RPA) com o modelo final da FEA-010

### Problema / Contexto

Após a entrega da FEA-010 (Líder RH) em 2026-05-21, o escopo da FEA-009 (Módulo RPA — próxima do roadmap) ficou desalinhado com o que efetivamente foi pra produção. Yuri pediu uma revisão pra entender se as mudanças impactariam a próxima feature.

### Diagnóstico

Cruzei o que estava em `docs/proposta-lider-e-rpa.md` + acceptance criteria do `prd.json` (FEA-009) contra o estado real pós-FEA-010. Levantei **9 pontos de impacto**:

- **Modelo do Líder RH mudou** (alto): escopo dizia `GESUSA.gestor=1`, mas a implementação final ficou em `GESGES.gestor=1` **por empresa**. `GESUSA.gestor` virou coluna zumbi (ver `memory/gesusa_gestor_zumbi.md`)
- **Filtro `id_tus<>1` virou padrão** (médio): toda query funcional precisa excluir internos da Leve, padrão estabelecido pelo hotfix `b0d433c`
- **Lista de menus padrão subiu pra 5 lugares** (médio): FEA-010 adicionou cópia de `updateGESMPR_menus` em `admin/iuds_pdo.php`. Risco real de esquecer um lugar ao incluir "Folha > Autônomos" e "Folha > RPA"
- **Empresa sem Líder RH no rollout** (médio): backfill é manual via `/master/`, então no início do MVP existirão empresas sem Líder. Precisa decidir comportamento (default proposto: aprovação fica em fila)
- **Aceite digital do autônomo** (baixo): acceptance original previa "SMS/email" mas não há infra de SMS contratada — email-only, com campo `email` obrigatório em GESAUT
- Pontos sem impacto: migrations de GESAUT/GESRPA/GESRPACFG, telas, geração de PDF, export Excel, multi-tenant, config em `/admin/`

### Ações Executadas

1. Leitura do `docs/proposta-lider-e-rpa.md` e da seção FEA-009 do `prd.json` (linhas 280-307)
2. Grep das funções de Líder RH em `admin/iuds_pdo.php` pra confirmar padrão (`selectGESUSA_lideres_ativos:11402` já com filtro `id_tus<>1`)
3. Apresentação estruturada dos 9 pontos pro Yuri
4. Atualização do `docs/proposta-lider-e-rpa.md`:
   - Header marca reconciliação 2026-05-22
   - Seção "Solução" corrigida (modelo `GESGES.gestor` + aviso da coluna zumbi)
   - Nova seção **"Reconciliação com a FEA-010"** dentro da FEA-009 com 7 itens em tabela
   - Débito técnico: 5 lugares com paths exatos + sugestão de centralizar antes da FEA-009
5. Atualização do `prd.json`:
   - Acceptance 284 (GESAUT): adicionado campo `email` obrigatório
   - Acceptance 287 (migration menu): aviso sobre 5 lugares hardcoded + `upsertGESMPR_lider_menus`
   - Acceptance 294 (aprovação): query base com `GESGES.gestor=1` + filtro `id_tus<>1` + comportamento sem Líder
   - Acceptance 295 (aceite): só email, sem SMS, com pré-requisito de validar com Jéssica
   - `notes` da FEA-009: novo bloco "RECONCILIAÇÃO PÓS-FEA-010" com 5 pontos
6. Validação do JSON (`python3 -c "import json; json.load(open('prd.json'))"`)
7. Commit + push

### Arquivos Modificados

- `docs/proposta-lider-e-rpa.md`
- `prd.json`

### Commits

- `757a81e` docs(FEA-009): reconcilia escopo do RPA com modelo final do Líder RH

### Validação

- JSON do PRD válido
- Push para `origin/main` confirmado (`1d6b2f8..757a81e`)

### Pendências

- **A confirmar com Jéssica antes do MVP do RPA:**
  - Comportamento quando empresa não tem Líder RH cadastrado (default proposto: aprovação em fila)
  - Todos os autônomos da TRYP têm email? (necessário pro aceite digital, já que SMS está fora)
- **Decisão técnica a tomar antes de codificar a FEA-009:**
  - Antecipar centralização da lista de menus padrão (débito técnico nº 1 em `docs/pendencias.md`) — esforço ~½ dia, evita repetir o problema na FEA-009
- Próximo passo do roadmap continua sendo **FEA-009 (Módulo RPA)** com esforço estimado 2-3 semanas

---

## Sessão 2: FEA-011 — Download em lote de documentos do colaborador (ZIP)

### Problema / Contexto

Cliente reportou necessidade de baixar todos os documentos de um colaborador de uma vez (holerites, IRRF, ponto, recibos diversos, documentos cadastrais, atestados) para entrega física ou arquivamento. Hoje o download é um a um, em uma aba que pode listar dezenas de PDFs. Solução: checkbox por linha + "selecionar todos" + botão "Baixar selecionados (.zip)" gerando ZIP em streaming. Escopo: um colaborador por vez (volume típico 40–60 PDFs / 50–200 MB).

### Diagnóstico

- Tabela da aba Documentos em `admin/alterar_colaborador.php` já tinha `data-token` por linha — reaproveitável como `value` do checkbox sem mudar o modelo de segurança existente
- O `<form id="form">` pai envolve a tela inteira; submit normal causaria conflito → solução foi `fetch` + `blob` no JS pra disparar o download sem submeter o form pai
- Seis fontes de documento, cada uma com mapeamento próprio de path no bucket (`upload/beneficios/holerite/{raiz_cnpj}/{arquivo}`, IRRF, Ponto, Diversos, GESDCOL, Atestado). Centralizado em helper único `resolveDocumentoPath` em `iuds_pdo.php`
- Bucket `gestou-uploads-489010` montado via gcsfuse em `/var/www/html/upload` → `ZipArchive::addFile` consegue ler direto, sem download intermediário

### Ações Executadas

1. Plano detalhado em `docs/plano-fea-011-download-documentos.md` (262 linhas) com escopo, decisões fechadas e validação
2. UI em `admin/alterar_colaborador.php`: coluna de checkbox em `<thead>` + cada `<tr>` do `<tbody class="recibos">`, "selecionar todos", botão "Baixar selecionados (.zip)" ao lado do "Incluir", contador "X selecionado(s)"
3. Endpoint `admin/controller/documentos_zip_post.php` (140 linhas):
   - Valida cada token contra `$_SESSION['alterar_colaborador']['token'][$token]` (HTTP 400 se inválido)
   - Resolve path via `resolveDocumentoPath` (com revalidação de `id_emp` na sessão)
   - Soma `filesize()` antes de iniciar ZIP — HTTP 413 se exceder 200 arquivos ou 500 MB
   - `set_time_limit(180)` + `ini_set('memory_limit', '512M')`
   - `ZipArchive` em `tempnam` → `readfile` → `unlink` em `try/finally`
   - `error_log` com `id_emp`, `id_usu`, `id_usa`, qtd e tamanho total (Cloud Logging)
4. Helper `resolveDocumentoPath` em `admin/iuds_pdo.php` (30 linhas) — mapeamento tipo → path
5. Validação local (Yuri) — fluxo OK
6. Commit `5fe0c3d` + push para `origin/main`
7. Após retomar a sessão: deploy em produção (build + push + `gcloud run deploy`)
8. Atualização do `prd.json`: FEA-011 → `passes: true` + nota com data/commit/revisão
9. Validação do JSON (`python3 -c "import json; json.load(open('prd.json'))"`)

### Arquivos Modificados

- `admin/alterar_colaborador.php`
- `admin/controller/documentos_zip_post.php` (novo)
- `admin/iuds_pdo.php`
- `docs/pendencias.md`
- `docs/plano-fea-011-download-documentos.md` (novo)
- `prd.json`

### Commits

- `5fe0c3d` feat(FEA-011): download em lote de documentos do colaborador (ZIP)

### Validação

- Validação funcional local pelo Yuri antes do deploy (checkbox, seleção, download do ZIP)
- Build Docker OK (todas as camadas em cache, image `sha256:cb7ce2b3091f...`)
- Push OK para Artifact Registry
- Deploy Cloud Run: revisão **`gestou-00140-dj2`** servindo 100% do tráfego
  - Warning benigno de IAM policy (binding `allUsers` já existia da FEA-010, ignorável)
- URL: https://gestou.com.br
- **Validação em prod (pós-deploy)**: download do ZIP OK. Latência levemente maior que local — esperado: bucket montado via gcsfuse adiciona round-trip ao GCS em cada `filesize()` + `ZipArchive::addFile`. Baseline pra comparar caso o cliente reporte lentidão no futuro

### Pendências

- Monitorar primeira execução real do cliente em prod (uso típico esperado de 40–60 arquivos)
- Acompanhar `error_log` no Cloud Logging pra detectar HTTP 413 (sinal pra revisar limite)
- Se a latência incomodar com volumes maiores, considerar: (a) cachear `filesize()` antes do loop, (b) `ZipArchive::addFromString` com `file_get_contents` paralelo, ou (c) gerar ZIP em outro serviço async
- Não há nada bloqueando o início da FEA-009 (RPA) — próximo na fila
